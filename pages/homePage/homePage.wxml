<!--homePage.wxml-->
<view class="homePage">
  <view class="cell" wx:for="{{items}}" wx:key="{{item.id}}" hidden="{{item.hidden}}" bindtap="tapHandler" id="{{item.id}}">
    <image class="cellIcon" mode="scaleToFill" src="{{item.src}}"></image>
  </view>
</view>